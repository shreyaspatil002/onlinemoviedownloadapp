<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Code:</strong>
  <%= @category.code %>
</p>

 <%= image_tag @category.main_image, width: '500' if @category.main_image.attached? %> 
 <br> 

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
